#include<iostream>

int mem[100][3][2];
int N, K;
int pasta[100];

int dfs(int n, int k, int b)
{
  if(mem[i][k][b] != 0)
    return mem[i][k][b];

  if(b == 0)
  {
    for(int i = 1; i <=3; i++)
    {
    }
  }
}

int main()
{
  cin >> N >> K;
  for(int i = 0; i < K; i++)
  {
    int a, b;
    cin >> a >> b;
    pasta[a] = b;
  }
}

